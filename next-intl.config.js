/** @type {import('next-intl').NextIntlConfig} */
const config = {
  locales: ["ru", "en", "tr"],
  defaultLocale: "en",
  localePrefix: "always",
  localeDetection: false,
};

module.exports = config;
